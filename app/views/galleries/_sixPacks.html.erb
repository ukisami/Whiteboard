<ul id="sixpack">
<% sorted_galleries.each do |gallery| %>
  <li>
    <a href="<%=h gallery_path(gallery, :offset=>offsetIndex, :sort=>sort_by) %>">
      <img src="<%=h gallery.thumbnail %>" />
      <span><%=h gallery.title %></span>
    </a>
  </li>
<% end %>
</ul>
